# Path where Boost 1.82 shared libraries live
LD_LIBRARY_PATH=/apps/fftw/3.3.6-pl2/lib:/apps/scotch/7.0.3/lib:/apps/boost/1.82.0/lib:/apps/qt/5.15.2/lib:/apps/vtk/9.1.0/lib:/home/hwustenb/code/nektar-legacy/build-master/ThirdParty

# Path where NekPy (Nektar++ Python bindings) are built
PYTHONPATH=$PYTHONPATH

# Optional: If your script supports sourcing a setup script at runtime
# (e.g., using the SET_ENV_SH_PATH env var as in the previous example)
SET_ENV_SH_PATH=/home/hwustenb/code/nektar-legacy/set_senv.sh

# Optional: If you have additional native deps (e.g., HDF5, VTK) provide their lib paths too
# LD_LIBRARY_PATH=/path/to/dep1/lib:/path/to/dep2/lib:$LD_LIBRARY_PATH

# Optional: If you use a non-system Python, point explicitly to it (rarely needed by PyCharm)
# PYTHON_EXECUTABLE=/usr/bin/python3.11
